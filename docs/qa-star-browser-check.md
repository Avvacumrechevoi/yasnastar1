# QA check: /star after backend migration

- Экран `/star` открывается без ошибок и показывает список Ясен, список механик и центральную звезду.
- Страница больше не выглядит пустой после перевода на backend API.
- При выборе механики `Опорный` счётчик меняется на `1/14`, в SVG появляется подсветка механики, а правый инспектор показывает описание активной механики.
- В инспекторе отображается серверно-вычисленный контекст для выбранной полочки и выбранной механики, то есть семантика не остаётся в дефолтном фронтенд-состоянии.
- Визуальный слой `/star` после миграции сохраняет работоспособность.
